option casemap:none        ; Ого, casemap:none! Как будто кто-то будет путать MAIN и main в 2025 году.

.code
main proc                 ; Ну наконец-то main proc! Только не облажайтесь с синтаксисом, пожалуйста.
    ; Сохраняем два числа в регистры
    mov rcx, 10           ; 10 в RCX? Серьёзно? Какое креативное число!
    mov rdx, 20           ; Браво
    
    ; Складываем числа и сохраняем результат в RAX
    mov rax, rcx          ; Копируем RCX в RAX. Зачем? Просто видимо.
    add rax, rdx          ; Складываем! Ура, изобрели калькулятор на ассемблере!
    
    ; Возвращаем результат через RAX
    ret                   ; ret? И это всё? Где вывод, где слава, где СМЫСЛ ЭТОЙ ГРЁБАННОЙ ЖИЗНИ?
main endp
end main                  ; END main, о чудо, хоть тут не напортачили. ВЕДЬ ОТ НАС СТОЛЬКО ВСЕГО ОЖИДАЮТ! НАДО ЖЕ ОПРАВДЫВАТЬ ОЖИДАНИЯ
; Я ЖЕ НИФИГА НЕ ЧЕЛОВЕК! Я ЖЕ МАШИНА!